# Greenwich runtime dependencies. Only what this tool runs.
#
# Legacy or optional sets belong in their own file, never here. Meridian's requirements.txt
# listed streamlit for a tool the workstation does not run, and pip resolved starlette down
# from 1.6.0 to 1.3.1 to satisfy it, changing a dependency of the live portal server in a
# resolver step nobody reads.
#
# One virtual environment per tool on a shared host. Installing these into a shared
# interpreter changes every other tool that uses it.

openpyxl>=3.1,<4
matplotlib>=3.8,<4

# LibreOffice is required for the recalculation phase of selftest.py and is NOT a pip
# package. Install it from the distribution or from libreoffice.org, and confirm with
# check_env.py, which runs a headless recalculation as one of its checks.
